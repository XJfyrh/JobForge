# S0 本地主模型试验结论

> 2026-09-16后续决策：维护者明确采用云端主chat，本地chat改为可选扩展，详见[当前路线](../plans/agent-execution-roadmap-v3.md#5-模型安全和费用从第一条链路开始具备)。下文保留原本地试验及当时结论；其中“主模型未选定/阻塞”不再表示需继续寻找本地模型或阻挡S1、契约PR。云端仍须实际接入与业务验收。

**主模型尚未选定。** 本次最多两个免费本地 chat 候选已实际运行，未满足有界工具循环及结果引用检查；不以确定性测试或模型能力标签替代通过。

| 候选/轮次 | 结果 | 原始证据 |
|---|---|---|
| qwen3:4b 冷启动 | 1 次 chat，60.016s 超时，usage unknown，没有工具派发 | [冷启动失败](agent-v3-model-probe-2026-09-16-qwen3-4b.json) |
| qwen3:4b 已加载复测 | 1 次 chat，60s 超时；后端缓存命中后仍未完成 | [预热失败](agent-v3-model-probe-2026-09-16-qwen3-4b-warm.json) |
| qwen3:1.7b | 16 次 chat，单次最长 22.734s；完整工具循环 0/3，通过单步纠正 2/2，独立结构化结果引用检查失败 | [完整基线](agent-v3-model-probe-2026-09-16-qwen3-1_7b.json) |

4b 的失败不只是冷加载；1.7b 可实际调用预注册工具，但仍发生多工具消息、错误终止与缺失/不一致证据。独立 `format=FINAL_SCHEMA` 虽产生可解析 JSON，进一步值/引用检查仍失败。因此没有进行“仅修正最终 JSON”的额外试验，也没有下载第三个模型或放宽 60s/180s。

工具均为确定性合成 fixture，尚未经过真实业务 HTTP、PostgreSQL 或向量检索。这是 S0 模型协议证据，不是 S1/S2 业务或 60 案例质量验收。12 项守卫测试、Ruff、mypy 通过，仅说明探针代码和拒绝边界得到验证。

**可继续 S1 真实业务服务、政策检索与固定流程；S2 主模型真实验收保持阻塞。** 一个已核实官方能力的远程备选是百炼北京区 `qwen3.7-flash-2026-07-15`，尚未授权费用或调用；需要明确服务/地域、账号模型权限、金额上限 B 和持久调用额度，Key 仅通过本机环境注入。

[完整说明及可直接运行的 digest/11435 复现命令](agent-v3-model-probe-2026-09-16.md)包含资源点采样、取消后确认空闲再复测的证据、费用算例及清理边界。两个新模型均已卸载推理内存并保留权重缓存，原生 Ollama 调查时自动启动的进程也已确认归属后停止。没有修改原服务配置或调用收费接口。

## 审查后的探针修复

上述真实模型结果来自提交 `18a9302` 的基线探针，源码 SHA-256 为 `fed93f10ccf151824ea8052a551e9c8481d97b27d35eeb7cb90f4ae93af1e8c5`；三份历史 JSON 保持原值，没有重新运行模型或替换负证据。

后续只读审查发现：structured/correction 阶段完成未知后可能继续推理，以及 `/api/ps` 失败可能阻止 case 结果落盘。修正版统一记录物理调用的 `completion_unknown`，每阶段先写证据、未知立即停止；可选状态采样在结果落盘后执行，失败只附加有界诊断。没有改变模型、提示词、调用次数或时间预算。

修正版源码 SHA-256：`d23e13cdfc921691f36e0f45b155219a844335c6c38825e7b9890e68c3e0874a`。新增回归覆盖全部阶段停止、传输/解析/大小失败、已完成但协议无效的区别，以及状态采样失败时成功/失败证据均保留。最终 **27 项确定性测试通过，Ruff check/format、mypy 通过**；修正版仅经回归测试，尚未重新执行真实模型。这不改变本地主模型未选定的结论。
