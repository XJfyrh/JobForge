# Optional loopback access for Go acceptance tests running on the host.
# Combine with compose.yaml; the regular models profile stays network-internal.
services:
  ollama:
    ports:
      - "127.0.0.1:${JOBFORGE_MODEL_TEST_PORT:-11435}:11434"
